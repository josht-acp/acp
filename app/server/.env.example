DATABASE_URL=postgres://acp:acp_dev@localhost:5432/acp
PORT=4000

# JWT — change this to a long random secret in production
JWT_SECRET=change-me-to-a-long-random-secret

# Resend SMTP (https://resend.com)
SMTP_HOST=smtp.resend.com
SMTP_PORT=465
SMTP_USER=resend
SMTP_PASS=re_xxxxxxxxxxxxxxxxxxxx
SMTP_FROM=portal@mail.ascensioncapital.com.au

# GP notification inbox
NOTIFY_EMAIL=jt@ascensioncapital.com.au

# Magic link base URL (no trailing slash)
APP_URL=http://localhost:3000
